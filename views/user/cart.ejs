<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Cart</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    <style>
      /* Keeping minimal necessary style for image fitting and sticky summary */
      .cart-img-v2 { 
        height: 100%; 
        width: 100%;
        object-fit: cover; 
      }
      .summary-sticky {
        position: sticky;
        top: 80px; /* Adjust top offset for better stickiness */
      }
      .product-row-v2 {
        border-bottom: 1px solid #eee;
      }
    </style>
  </head>
  <body class="bg-light">
    <nav class="navbar navbar-light bg-white shadow-sm sticky-top">
      <div class="container">
        <span class="navbar-brand mb-0 h1 text-primary">🛍️ E-Shop</span>
      </div>
    </nav>
    
    <main class="container py-5">
      <!-- flash messages -->
      <% if (success_msg && success_msg.length) { %>
        <% success_msg.forEach(msg => { %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% }) %>
      <% } %>

      <% if (error_msg && error_msg.length) { %>
        <% error_msg.forEach(msg => { %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% }) %>
      <% } %>
      <div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom">
        <h1 class="h2 fw-light mb-0">Your Shopping Cart</h1>
        <a href="/shop" class="btn btn-sm btn-outline-secondary">Continue Shopping &rarr;</a>
      </div>

      <div class="row g-4">
        <div class="col-12 col-lg-8">
          
          <% if (!cart || cart.length === 0) { %>
            <div class="card shadow-lg border-0 bg-white" style="min-height: 400px;">
              <div class="card-body d-flex flex-column align-items-center justify-content-center text-center p-5">
                <h3 class="mb-3 text-secondary">Your cart is empty 🙁</h3>
                <p class="text-muted mb-4">You haven't added any items yet. Let's find some great products!</p>
                <a href="/shop" class="btn btn-primary btn-lg rounded-pill px-5">Start Shopping Now</a>
              </div>
            </div>
          <% } else { %>
            
            <div class="card shadow-lg border-0 rounded-4 p-3 p-md-4">
              <% cart.forEach(item => { %>
                <div class="row g-0 align-items-center py-3 product-row-v2">
                  <div class="col-2 col-md-1">
                    <img 
                      src="<%= item.productId.image %>" 
                      class="img-fluid rounded-2 cart-img-v2" 
                      alt="<%= item.productId.name %>"
                      style="height: 80px; width: 80px;"
                    />
                  </div>
                  
                  <div class="col-5 col-md-6 px-3">
                    <h6 class="fw-bold mb-0 text-truncate"><%= item.productId.name %></h6>
                    <small class="text-muted d-none d-md-block">Unit Price: <span class="fw-semibold text-dark">₹<%= item.productId.price %></span></small>
                  </div>
                  
                  <div class="col-5 col-md-5 d-flex justify-content-between align-items-center ps-md-5">
                    
                    <div class="d-flex align-items-center border rounded-pill">
                      <form action="/cart/update/<%= item.productId._id %>" method="POST" class="d-inline-flex">
                        <button type="submit" name="action" value="decrease" class="btn btn-sm btn-light rounded-circle p-0" style="width: 32px; height: 32px;">&minus;</button>
                        <span class="px-2 align-self-center fw-bold small"><%= item.quantity %></span>
                        <button type="submit" name="action" value="increase" class="btn btn-sm btn-light rounded-circle p-0" style="width: 32px; height: 32px;">&plus;</button>
                      </form>
                    </div>

                    <div class="text-end">
                      <div class="fw-bolder fs-6 mb-1">₹<%= item.productId.price * item.quantity %></div>
                      <form action="/cart/remove/<%= item.productId._id %>" method="POST">
                        <button type="submit" class="btn btn-link btn-sm text-danger p-0" style="text-decoration: none;">Remove</button>
                      </form>
                    </div>

                  </div>
                </div>
              <% }) %>
              <style>
                .product-row-v2:last-of-type { border-bottom: none; }
              </style>
            </div>
          <% } %>
        </div>

        <div class="col-12 col-lg-4">
          <% const total = cart.reduce((sum, item) => sum + (item.productId.price * item.quantity), 0); %>
          
          <div class="card summary-sticky shadow-lg border-0 bg-white rounded-4">
            <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
              <h5 class="mb-0">Summary</h5>
            </div>
            
            <div class="card-body p-4">
              <ul class="list-group list-group-flush mb-4">
                <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                  <span class="text-muted">Subtotal</span>
                  <span class="fw-semibold">₹<%= total %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                  <span class="text-muted">Total Items</span>
                  <span class="fw-semibold text-dark"><%= cart.reduce((n, i) => n + i.quantity, 0) %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center fw-bold fs-5 text-success px-0 pt-3">
                  <span>Order Total</span>
                  <span>₹<%= total %></span>
                </li>
              </ul>

              <div class="d-grid gap-3">
                <form action="/checkout" method="POST">
                  <button type="submit" class="btn btn-primary btn-lg rounded-pill" <%= (!cart || cart.length === 0) ? 'disabled' : '' %>>
                    Secure Checkout
                  </button>
                </form>
                <a href="/shop" class="btn btn-outline-secondary rounded-pill">Find More Deals</a>
              </div>
            </div>
          </div>
          
          <div class="text-center mt-3 pt-2 text-muted small">
            <p class="mb-0">All prices are inclusive of taxes.</p>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>